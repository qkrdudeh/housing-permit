# 주택건설 대시보드 (분양비교 실험판)

국토교통부 주택건설실적통계 인허가·착공·준공·분양 통합 대시보드.

## 배포
Vercel에 GitHub 연동으로 배포합니다. `main` 브랜치에 push하면 자동으로 재배포됩니다.

## 갱신 방법
1. 새 `index.html`을 이 폴더에 덮어씁니다.
2. `git add -A && git commit -m "데이터 갱신 YYYY-MM" && git push`
3. Vercel이 자동으로 같은 URL에 재배포합니다.
